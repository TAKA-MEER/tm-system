# my_package

ROS2 Humbleパッケージのサンプルです。
ここにパッケージのコードを追加してください。

## ビルド

```bash
cd /ros_ws
colcon build --packages-select my_package --symlink-install
```

## 実行

```bash
source /ros_ws/install/setup.bash
ros2 run my_package <node_name>
```
